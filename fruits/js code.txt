var particleMaterial = new THREE.MeshBasicMaterial();
		particleMaterial.map = THREE.ImageUtils.loadTexture('appleD.jpg');
		particleMaterial.side = THREE.DoubleSide;
		
		
		var loader = new THREE.JSONLoader();
        loader.load( 'apple.js', function ( geometry ) {
        var mesh = new THREE.Mesh( geometry, particleMaterial );
		//var itmArr = [];

            mesh.position.x =0;
            mesh.position.y =0;
            mesh.position.z =0;
			scene.add( mesh );
			
        }); 